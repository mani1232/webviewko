#ifndef BRIDGE_H
#define BRIDGE_H

// Define WEBVIEW_HEADER to hide C++ implementation details (like templates)
// from the cinterop tool. This prevents the "ClassTemplatePartialSpecialization" crash.
#define WEBVIEW_HEADER

#include "webview.h"

#endif